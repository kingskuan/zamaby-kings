{
  "name": "privatefund-frontend",
  "version": "1.0.0",
  "description": "PrivateFund dApp — FHE Private Payroll Protocol (static frontend)",
  "scripts": {
    "start": "npx serve frontend -p ${PORT:-3000}"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
